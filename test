services:
  runner:
    image: gitlab/gitlab-runner:alpine3.21-v18.10.1
    restart: always
    environment:
      DOCKER_VOLUMES: "/var/run/docker.sock:/var/run/docker.sock"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./runner-config:/etc/gitlab-runner